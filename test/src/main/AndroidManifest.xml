<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.acecoder.test">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidJavaDemo">

        <activity android:name=".jetpack.lifecycle.LifecycleTestActivity"></activity>

        <activity android:name=".jetpack.livedata.LiveDataTestActivity"></activity>

        <activity android:name=".jetpack.viewmodel.ViewModelTestActivity"></activity>

    </application>

</manifest>